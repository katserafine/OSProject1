//project 1 for 4348,  processes

//First interpretation depending on the
//  input there is  certaint outcome

//there are mutliple lines of input and therefore 
//  the program much loop through all lines of input


//need to read a text file line by line in JAVA

//test

//need a timer to simulate a timed execution of inturpt handler
public class project1 {

    //this will need to change to take in files for inpu and output
    public static void main(String[] args){

   
        bool moreLines = true;

        int x;
        int y;

        int[] memoryArray = new int[2000];

        while(moreLines == true){


            //each instruction should be executed before the 
            //  before the next instruction is fetched
            // Read in line (execution)
            // read in input (variable)
            
            //int input = readinline command
            //int = readinline variable


            //Two different variables X and Y exist
            //Must account for this

        

            int input = 0;

            Switch(input) {
                case 1:
                    //Load value

                    break;
                case 2:
                //load address

                    break;
                
                case 3:
                    //load fromindex address
                    //get from stack data structure (stack = index)

                    break;

                case 4:

                    //load the value at address + x
                    //
                    break;
                case 5:
                    //load 

            }
        }
    }
}